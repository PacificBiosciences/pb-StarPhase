
/// the main CLI module
pub mod core;
/// the diplotype CLI subcommand for calling the diplotypes
pub mod diplotype;
/// The build CLI subcommand for constructing our local database via CPIC API
pub mod db_build;
